# PostgreSQL Database Configuration
# Update with YOUR local PostgreSQL credentials

# Option 1: If using default PostgreSQL user (postgres)
DATABASE_URL="postgresql://postgres:your_postgres_password@localhost:5432/dcms_local"

# Option 2: If using a custom database user (recommended)
DATABASE_URL="postgresql://dcms_user:your_password@localhost:5432/dcms_local"

# Session encryption key (use any random string, at least 32 characters)
SESSION_SECRET="your-super-secret-key-here-make-it-long-at-least-32-chars"

# Environment mode (leave as development for local work)
NODE_ENV="development"

# ============================================
# INSTRUCTIONS:
# ============================================
# 1. Copy this file: cp .env.local.example .env.local
# 2. Edit .env.local with YOUR PostgreSQL details
# 3. Create the database: createdb dcms_local
# 4. Create user (optional): createuser dcms_user
# 5. Run: npm run db:push
# 6. Run: npm run dev
# 
# DO NOT COMMIT .env.local TO GIT
# ============================================
